import * as User from "./User";
import * as Query from "./Query";
import * as Mutation from "./Mutation";
import * as Roundtable from "./Roundtable";

export const resolvers = {
	User,
	Roundtable,
	Query,
	Mutation,
};

